This directory will contain any information required for any
services that are being utilized.

Note if data is being returned by a service, the data should
be retrieved via a gateway and a class.  Not sure if this is
right.
